name: one_time_executor
description: "A lightweight Flutter library to execute actions only once with pluggable storage adapters."
version: 0.0.1
repository: https://github.com/changjoo-park/one_time_executor
homepage: https://github.com/changjoo-park/one_time_executor

environment:
  sdk: ^3.7.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  mockito: ^5.4.4
  build_runner: ^2.4.8
